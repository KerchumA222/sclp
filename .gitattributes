# Normalize line endings: LF in the repo, auto-detect text vs binary.
* text=auto eol=lf

# Treat known binary types as binary (never normalize/diff as text).
*.gguf binary
*.bin binary
*.so binary
*.o binary
*.a binary
*.dat binary
